// import { getAlcoholHistory } from '../../services/alcohol';
// import { fetchApi } from '../../utils/fetch-api';

test('test', () => {
  // jest.mock(fetchApi);
  // getAlcoholHistory();
  expect(1).toEqual(1);
});
